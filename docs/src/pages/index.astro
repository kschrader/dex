---
import Base from '../layouts/Base.astro';
import Terminal from '../components/Terminal.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<Base title="Home" description="Task tracking for LLM workflows. Persistent memory for complex, multi-session work.">
  <div class="hero">
    <p class="tagline">Task tracking for LLM workflows</p>
    <p class="tagline">Persistent memory for complex, multi-session work</p>

    <div class="hero-prompt">
      <div class="prompt-box">
        <span class="prompt-caret">&gt;</span>
        <span class="prompt-text">Use Dex. Work on the next logical task. When done, run code-simplifier and commit.</span>
      </div>
    </div>

    <div class="cta-buttons">
      <a href={`${base}/install`} class="btn btn-primary">Get Started</a>
    </div>
  </div>

  <section class="section">
    <h2>Quick Start</h2>
    <p>Install the dex skill for Claude Code:</p>
    <Terminal title="Terminal">
      <pre class="cli-output"><span class="cli-dim">$</span> claude mcp add dex -- npx -y @zeeg/dex
<span class="cli-green">Added</span> MCP server dex</pre>
    </Terminal>
    <p>Or with <a href="https://github.com/anthropics/skills">skills</a>:</p>
    <Terminal title="Terminal">
      <pre class="cli-output"><span class="cli-dim">$</span> npx skills add dcramer/dex</pre>
    </Terminal>
    <p>Restart Claude Code, then use <code>/dex</code> to manage tasks.</p>
  </section>

  <section class="section scenarios">
    <div class="scenario">
      <div class="scenario-prompt">
        <span class="prompt-label">You say:</span>
        <p>"Migrate our API from Express to Hono. Use dex to track it."</p>
      </div>
      <Terminal title="Terminal">
        <pre class="cli-output"><span class="cli-dim">$</span> dex create -d "Migrate auth middleware" --context "Current auth
uses passport.js with JWT strategy in src/middleware/auth.ts.
Hono equivalent is hono/jwt. Must preserve req.user pattern
that 12 route handlers depend on."
<span class="cli-green">Created</span> task <span class="cli-bold">p7n4j9q2</span>
<span class="cli-yellow">[ ]</span> <span class="cli-bold">p7n4j9q2</span>: Migrate auth middleware

<span class="cli-bold">Context:</span>
  Current auth uses passport.js with JWT strategy in
  src/middleware/auth.ts. Hono equivalent is hono/jwt. Must
  preserve req.user pattern that 12 route handlers depend on.</pre>
      </Terminal>
    </div>

    <div class="scenario">
      <div class="scenario-prompt">
        <span class="prompt-label">Next session:</span>
        <p>"Continue working on the Hono migration."</p>
      </div>
      <Terminal title="Terminal">
        <pre class="cli-output"><span class="cli-dim">$</span> dex list
<span class="cli-yellow">[ ]</span> <span class="cli-bold">p7n4j9q2</span>: Migrate auth middleware
<span class="cli-yellow">[ ]</span> <span class="cli-bold">w5t2v8b3</span> <span class="cli-red">[B: p7n4j9q2]</span>: Migrate route handlers to Hono

<span class="cli-dim">$</span> dex show p7n4j9q2
<span class="cli-yellow">[ ]</span> <span class="cli-bold">p7n4j9q2</span>: Migrate auth middleware

<span class="cli-bold">Context:</span>
  Current auth uses passport.js with JWT strategy in
  src/middleware/auth.ts. Hono equivalent is hono/jwt. Must
  preserve req.user pattern that 12 route handlers depend on.</pre>
      </Terminal>
    </div>

    <div class="scenario">
      <div class="scenario-prompt">
        <span class="prompt-label">When done:</span>
        <p>"Auth middleware is migrated. Record what changed."</p>
      </div>
      <Terminal title="Terminal">
        <pre class="cli-output"><span class="cli-dim">$</span> dex complete p7n4j9q2 -r "Replaced passport.js with hono/jwt.
User context now accessed via c.get('user') instead of req.user.
Added middleware to src/middleware/auth.ts. Tests passing."
<span class="cli-green">Completed</span> task <span class="cli-bold">p7n4j9q2</span>
<span class="cli-green">[x]</span> <span class="cli-bold">p7n4j9q2</span>: Migrate auth middleware

<span class="cli-bold">Context:</span>
  Current auth uses passport.js with JWT strategy in
  src/middleware/auth.ts. Hono equivalent is hono/jwt.

<span class="cli-bold">Result:</span>
  <span class="cli-green">Replaced passport.js with hono/jwt. User context now accessed
  via c.get('user') instead of req.user. Tests passing.</span></pre>
      </Terminal>
    </div>
  </section>
</Base>

<style>
  .hero-prompt {
    max-width: 100%;
    margin: 2rem 0;
  }

  .prompt-box {
    font-family: var(--font-mono);
    font-size: 1rem;
    line-height: 1.6;
    text-align: left;
    display: flex;
    align-items: baseline;
    justify-content: center;
  }

  .prompt-caret {
    color: var(--text);
    margin-right: 0.5rem;
    flex-shrink: 0;
  }

  .prompt-text {
    color: var(--text);
    overflow: hidden;
    white-space: nowrap;
    border-right: 2px solid var(--text);
    width: 0;
    animation:
      typewriter 2.5s steps(80) 0.5s forwards,
      blink 0.75s step-end infinite;
  }

  @keyframes typewriter {
    to {
      width: 100%;
    }
  }

  @keyframes blink {
    50% {
      border-color: transparent;
    }
  }

  /* Scenarios */
  .scenario {
    margin-bottom: 3rem;
  }

  .scenario:last-child {
    margin-bottom: 0;
  }

  .scenario-prompt {
    margin-bottom: 1rem;
  }

  .prompt-label {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-tertiary);
    display: block;
    margin-bottom: 0.5rem;
  }

  .scenario-prompt p {
    font-size: 1.125rem;
    line-height: 1.5;
    color: var(--text);
    margin: 0;
  }

  /* CLI output colors */
  .cli-output {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    line-height: 1.6;
    color: #e5e5e5;
    margin: 0;
    white-space: pre-wrap;
    word-break: break-word;
  }

  .cli-green {
    color: #4ade80;
  }

  .cli-yellow {
    color: #facc15;
  }

  .cli-cyan {
    color: #22d3ee;
  }

  .cli-red {
    color: #f87171;
  }

  .cli-bold {
    font-weight: 600;
    color: #fff;
  }

  .cli-dim {
    color: #737373;
  }
</style>
